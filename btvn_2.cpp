#include <iostream>
 using namespace  std;

 int main(){
     printf("hihi");
     int n, a, b;
     float x;
     cin >> n >> a >> b;
     x = n / (a - b) ;
    //  if (x - (n / (a - b)) == 0)
    //  {
    //      num_days == x;
    //  }else{
    //      num_days == x + 1;
    //  }
        
     printf("%f", x);
     
     return 0;
 